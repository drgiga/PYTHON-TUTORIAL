"""from pynput.keyboard import Key, Listener

def runner(key):
    #print('{0} pressed'.format(
        #key))
    check_key(key)
text = ""
def input_user():
    
    #while True:
        line = input('for exit write : exit \n> ')
        #if line == 'exit':
            #break
        text += line
    #print(line)
    #print('Exit Done!')
print(text)


def check_key(key):
    if key in [Key.enter,input_user]: 
        if(key == Key.enter):
            print('Enter')
        
        else:
            print("no arrow key")



# Collect events until released
with Listener(runner=runner) as listener:
    listener.join()
"""
        

